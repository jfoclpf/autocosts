The html and jpg statistical tables for the automobile costs for each country, 
are generated through and deployed to `bin/` upon building the project, 
namely using the command `node build.js --genTables`
